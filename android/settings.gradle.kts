pluginManagement {
    val flutterSdkPath = run {
        val properties = java.util.Properties()
        file("local.properties").inputStream().use { properties.load(it) }
        val flutterSdkPath = properties.getProperty("flutter.sdk")
        require(flutterSdkPath != null) { "flutter.sdk not set in local.properties" }
        flutterSdkPath
    }

    includeBuild("$flutterSdkPath/packages/flutter_tools/gradle")

    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

plugins {
    id("dev.flutter.flutter-plugin-loader") version "1.0.0"
    // Bump Android Gradle Plugin to support Kotlin 2.3.x compiler options
    id("com.android.application") version "8.11.1" apply false
    // START: FlutterFire Configuration
    id("com.google.gms.google-services") version("4.3.15") apply false
    // END: FlutterFire Configuration
    // Bump Kotlin plugin to match plugin artifacts (kotlin-stdlib 2.3.x used by plugins)
    // Updated from 2.0.0 to 2.3.10 to resolve incompatible Kotlin metadata errors.
    id("org.jetbrains.kotlin.android") version "2.3.10" apply false
}

include(":app")